<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.usb.accessory" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.software.companion_device_setup" android:required="false" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" tools:ignore="SmsAndCallLogPolicy" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />

    <!-- wireless connection -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />

    <!-- wireless auto connect -->
    <uses-permission android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_RUN_IN_BACKGROUND" />

    <!-- needed for LAN access (TCP server and wireless connection) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- needed to launch arbitrary apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <application
        android:name=".Geargrinder"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Geargrinder">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_title"
            android:exported="false"
            android:launchMode="singleInstance"
            />

        <activity
            android:name=".DebugActivity"
            android:label="@string/debug_title"
            android:exported="true"
            android:launchMode="singleInstance"
            />

        <activity
            android:name=".ConnectionRequestActivity"
            android:exported="false"
            android:launchMode="standard"
            android:theme="@style/Theme.Geargrinder.Invisible"
            >
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_DETACHED" />
                <action android:name="io.benwiegand.projection.geargrinder.USB_PERMISSION" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"
                android:resource="@xml/usb_accessory_filter"
                />
        </activity>

        <activity
            android:name=".ProjectionActivity"
            android:exported="true"
            android:configChanges="keyboard|keyboardHidden|screenSize|screenLayout|orientation|smallestScreenSize"
            android:resizeableActivity="true"
            android:launchMode="singleInstance"
            android:taskAffinity="io.benwiegand.projection.geargrinder.Projection"
            android:theme="@style/Theme.Geargrinder.Projection"
            />

        <activity
            android:name=".VirtualActivityLauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.Geargrinder.Projection"
            />

        <activity
            android:name=".PermissionGrantActivity"
            android:label="@string/permission_grant_activity_title"
            android:exported="false"
            />

        <activity
            android:name=".ScreenWakeActivity"
            android:exported="false"
            android:taskAffinity="io.benwiegand.projection.geargrinder.ScreenWakeActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Geargrinder.ScreenWakeActivity"
            />

        <service
            android:name=".ConnectionService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|mediaProjection"
            />

        <service
            android:name=".BluetoothCompanionService"
            android:exported="true"
            android:permission="android.permission.BIND_COMPANION_DEVICE_SERVICE"
            tools:targetApi="31"
            >
            <intent-filter>
                <action android:name="android.companion.CompanionDeviceService" />
            </intent-filter>
        </service>

        <service
            android:name=".AccessibilityInputService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            tools:ignore="AccessibilityPolicy">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_meta" />
        </service>

        <service
            android:name=".PackageService"
            android:exported="false"
            />

        <service
            android:name=".PrivdService"
            android:exported="false"
            />

        <service
            android:name=".NotificationService"
            android:exported="false"
            android:label="@string/notification_service_permission_rationale"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <meta-data
                android:name="android.service.notification.default_filter_types"
                android:value="conversations|alerting">
            </meta-data>
            <meta-data
                android:name="android.service.notification.disabled_filter_types"
                android:value="silent|ongoing">
            </meta-data>
        </service>


        <receiver
            android:name=".PrivdConnectionReceiver"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            >
            <intent-filter>
                <action android:name="io.benwiegand.projection.geargrinder.BIND_PRIVD" />
            </intent-filter>
        </receiver>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            />

    </application>

</manifest>