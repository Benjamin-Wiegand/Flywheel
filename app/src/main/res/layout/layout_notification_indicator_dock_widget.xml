<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="48dp"
    android:layout_height="48dp"
    android:padding="4dp"
    android:background="@drawable/selectable_item_background_circular"
    android:focusable="true"
    android:clickable="true"
    >

    <FrameLayout
        android:id="@+id/notification_counter_indicator"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:minWidth="24dp"
        android:layout_gravity="center"
        android:background="@drawable/circle"
        android:backgroundTint="?colorDockForeground"
        android:visibility="gone"
        tools:visibility="visible"
        >

        <TextView
            android:id="@+id/notification_counter_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:paddingHorizontal="6dp"
            android:textColor="?colorDockBackground"
            android:textAlignment="center"
            android:maxLines="1"
            android:ellipsize="end"
            style="?textAppearanceButton"
            tools:text="69"
            />

    </FrameLayout>

    <ImageView
        android:id="@+id/notification_bell_indicator"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center"
        android:src="@drawable/notification_bell"
        app:tint="?colorDockForeground"
        android:importantForAccessibility="no"
        />


</FrameLayout>