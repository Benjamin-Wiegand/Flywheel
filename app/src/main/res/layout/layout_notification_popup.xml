<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/popup_notification_background">

    <LinearLayout
        android:id="@+id/touch_target"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:elevation="8dp"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        >

        <ImageView
            android:id="@+id/notification_small_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:padding="4dp"
            android:background="@drawable/circle"
            android:backgroundTint="?android:attr/colorForeground"
            android:importantForAccessibility="no"
            android:visibility="gone"
            app:tint="?android:attr/colorBackground"
            tools:src="@drawable/car"
            tools:visibility="visible"
            />

        <ImageView
            android:id="@+id/notification_large_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:importantForAccessibility="no"
            android:visibility="gone"
            tools:src="@mipmap/ic_launcher"
            />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="8dp"
            >

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                >

                <Space
                    android:layout_width="0dp"
                    android:layout_height="24dp"
                    />

                <TextView
                    android:id="@+id/notification_top_line_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:lines="1"
                    android:ellipsize="end"
                    tools:text="Some app · now"
                    style="?attr/textAppearanceCaption"
                    />

            </LinearLayout>

            <TextView
                android:id="@+id/notification_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:lines="1"
                android:ellipsize="end"
                tools:text="Some notification title"
                style="?attr/textAppearanceSubtitle1"
                />

            <TextView
                android:id="@+id/notification_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:lines="1"
                android:ellipsize="end"
                tools:text="Some notification text\nsecond line should not be visible"
                style="?attr/textAppearanceBody1"
                />

        </LinearLayout>

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        >

        <Button
            android:id="@+id/speak_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="@string/speak_button"
            android:drawableLeft="@android:drawable/ic_lock_silent_mode_off"
            android:textColor="?android:attr/colorForeground"
            tools:ignore="RtlHardcoded"
            style="?attr/borderlessButtonStyle"
            />

        <Button
            android:id="@+id/clear_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="@string/clear_button"
            android:drawableLeft="@android:drawable/ic_menu_close_clear_cancel"
            android:textColor="?android:attr/colorForeground"
            tools:ignore="RtlHardcoded"
            style="?attr/borderlessButtonStyle"
            />

    </LinearLayout>

</LinearLayout>